window.api = "http://localhost:8080/admin";

window.brandAPI = api + "/brand";

window.categoryAPI = api + "/category";

window.sizeAPI = api + "/size";

window.colorAPI = api + "/color";

window.productAPI = api + "/product";

window.billAPI = api + "/bill";
